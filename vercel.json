{
  "version": 2,
  "builds": [
    { "src": "flask_app.py", "use": "@vercel/python", "config": {
        "maxLambdaSize": "450mb"
      } }
  ],
  "routes": [
    { "src": "/(.*)", "dest": "/flask_app.py" }
  ]
}
